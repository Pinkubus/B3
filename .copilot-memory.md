### Core Purpose
B3 is a VS Code extension that makes Copilot produce BBB playbooks so the user performs edits and commands one step at a time.

### Informal References & Nicknames
- "quickstart" -> `/QUICKSTART.md`
- "the instructions template" -> `/resources/copilot-instructions.template.md`
- "the /bbb prompt" -> `/resources/bbb.prompt.md`
- "extension activation" -> `/src/extension.ts`

### Active Context
- Enacted REVIEW-FEEDBACK.md: added `create` and `replace` step kinds, `after`/`before` drift-proof anchors on `edit`, and the `counted="false"` nav-step flag (parser + runner).
- Loosened `terminal`/`open`/`goto` verification to reduce false negatives; reconciled keybinding references; flipped template to "explain every line"; genericized code-quality rules; removed the dead "explain rendered as a comment" doc claim.
- Regenerated `meru-inventory-lesson/.bbb/playbook.md` to use `create` steps for file creation and expected-outcome terminal explains; verified end-to-end by reconstructing and running the app (endpoints + POST sync all correct).

### Not yet done (recommended follow-ups)
- #5 per-concept "I already know this" skip; #1 `/bbb-tour` read-only mode; #9 inline-comment explain insertion (doc claim removed instead); #15 multi-language teach highlighting; #13 `troubleshoot` block; #27 glossary.

### Structural Map
- `/src/extension.ts` - extension activation, command registration, Copilot customization install flow.
- `/src/playbookParser.ts` - parses `.bbb/playbook.md` steps and BBB comment directives.
- `/src/playbookRunner.ts` - runs and verifies playbook steps.
- `/resources/copilot-instructions.template.md` - installed instructions that make Copilot write playbooks.
- `/resources/bbb.prompt.md` - installed prompt surfaced as `/bbb` in Copilot Chat.